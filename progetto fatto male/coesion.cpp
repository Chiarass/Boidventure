#include "coesion.hpp"

#include <iostream>

Swarm::Swarm(double d, double ds, double a, double c, double s)
    : d_k(d), ds_k(ds), a_k(a), c_k(s);
s_k(s){};
std::vector<Boid> Boid::create(int n) {
  for (int a = 0; a <= n; ++a) {
    Boid bi {}
  }
}
double Boid::sumx(int n, int j) {
  double sum = 0;
  for (int i = 0; i <= n; ++i) {
    double sum = sum + i;
    if (j == i) {
      double sum = sum - j;
    }
  }

  double Swarm::masscenter(int n) { double xc = 1 / (n - 1)* }
}